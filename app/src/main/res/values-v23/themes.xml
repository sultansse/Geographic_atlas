<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Base.Theme.GeographicAtlas" parent="Theme.MaterialComponents.DayNight">
        <!-- Customize your light theme here. -->
 1
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white</item>

        <!-- AppBar color. -->
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowBackground">@color/white</item>

        <!-- Set icons color to black -->
        <item name="android:windowLightStatusBar">true</item>

        <!-- Customize the color of the title and icons in the AppBar -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>


        <item name="colorSecondary">@color/cardview_light_background</item>
        <item name="colorOnSecondary">@color/black</item>
    </style>
</resources>